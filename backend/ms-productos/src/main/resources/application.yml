server:
  port: 8083

spring:
  application:
    name: ms-productos

  datasource:
    url: jdbc:postgresql://localhost:5432/jqcommerce
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        format_sql: true